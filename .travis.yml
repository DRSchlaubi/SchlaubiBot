## Stupid line
language: java
sudo: false
script: mvn clean package
before_deploy:
  - git config --global user.email "builds@travis-ci.com"
  - git config --global user.name "Travis CI"
  - ls -R
deploy:
deploy:
  provider: releases
  api_key: "eeb0d82f469f0dbf642a5ffa3bdc2d598ff43990"
  file: "SchlaubiBot-1.0-SNAPSHOT.jar"
  skip_cleanup: true
  on:
   tags: false
   all_branches: true
    
